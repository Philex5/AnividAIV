/**
 * UI Components Index
 *
 * 统一导出所有 UI 组件
 */

export { Button, buttonVariants } from './button';
export { LinkingComponent, linkingVariants } from './linking';

// Re-export types
export type {
  MainButton,
  LinkButton,
  LinkingComponentProps,
  SceneConfig,
} from '@/types/components/linking';
